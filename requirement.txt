# Core ML / NLP
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.30.0
tokenizers>=0.13.0
sentencepiece>=0.1.98
datasets>=2.8.0
accelerate>=0.20.0

# Text preprocessing & utilities
numpy>=1.24.0
pandas>=2.0.0
nltk>=3.8.0
sentence-transformers>=2.2.2

# Audio processing (if you want to add audio examples or TTS)
librosa>=0.9.2
soundfile>=0.12.1

# Serving / demo
gradio>=3.30.0        # lightweight UI demo for interaction
fastapi>=0.95.0       # API server (optional)
uvicorn>=0.22.0       # ASGI server for FastAPI

# Optional: use OpenAI API instead of local models (comment out if unused)
openai>=1.0.0

# Config & env vars
python-dotenv>=1.0.0

# Helpful for evaluation / metrics / logging
tqdm>=4.65.0
scikit-learn>=1.2.2
wandb>=0.15.0         # optional experiment tracking (remove if not needed)
